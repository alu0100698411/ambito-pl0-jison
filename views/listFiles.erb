 <div class="picture"> <img src="<%= session[:image] %>" alt="Hey, it's me!"> </div>
 <ul class="FileList">
    <% files.each do |file| %>
      <li><a href="<%= file.name %>">><%= file.name %></a></li>
    <% end %>
  </ul>
